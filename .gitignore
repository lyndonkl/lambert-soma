# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Env & secrets
.env
.env.*
!.env.example

# macOS
.DS_Store

# Working notes — local by design; the public record is ADRs + EXPs (2026-08-29)
docs/mental-models/
spikes/

# Runtime state — agent runs are data, not source
runs/
logs/
telemetry/*.db
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# Beads: JSONL is source of truth; the sqlite cache is derived
.beads/*.db

# Model artifacts
*.gguf
*.safetensors
